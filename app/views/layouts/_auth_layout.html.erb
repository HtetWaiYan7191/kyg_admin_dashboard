<% if is_sign_in_page?%>
    <div class="d-flex justify-content-center ">
        <%= render 'shared/noti' %>
    </div>
<% end %>
<section class="container-fluid  p-4 d-flex   align-items-center min-vh-75 <%= center_auth_layout %> ">
    <div class=" <%= is_sign_in_page? ? 'border p-2': ''%>" style="width: 60%">
        <%= yield %>
    </div>
</section>