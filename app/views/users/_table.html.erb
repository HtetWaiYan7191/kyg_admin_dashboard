<table class="table align-middle mb-0 bg-white">
  <thead class="bg-light">
    <tr>
      <th>Name</th>
      <th>Title</th>
      <th>Department</th>
      <th>Position</th>
      <th>Actions</th>
    </tr>
  </thead>
  <tbody>
  <% locals[:users].each do |user| %>
    <tr>
      <td>
        <div class="d-flex align-items-center">
          <img
              src="https://mdbootstrap.com/img/new/avatars/8.jpg"
              alt=""
              style="width: 45px; height: 45px"
              class="rounded-circle"
              />
          <div class="ms-3">
            <p class="fw-bold mb-1"><%= user.name %> </p>
            <p class="text-muted mb-0"><%= user.email %> </p>
          </div>
        </div>
      </td>
      <td>
        <p class="fw-normal mb-1"><%= user.title %></p>
        <p class="text-muted mb-0"><%= user.role %> </p>

      </td>
      <td>
        <p class="fw-normal mb-1"><%= user.department %></p>
      </td>
      <td><%= user.position %></td>
      <td>
      <% if current_user.admin? %>
            <%= link_to 'Edit', edit_user_path(id: user.id), class: 'btn btn-link btn-sm btn-rounded' %>
            <%#= link_to 'Create', new_user_path, class: ' btn btn-link btn-sm btn-rouned' %>
            <%= link_to "Delete", user_path(id: user.id), { class: 'btn btn-link btn-sm btn-rounded', method: :delete } %>
      <% end %>

      </td>
    </tr>
  <% end %>
  </tbody>
</table>