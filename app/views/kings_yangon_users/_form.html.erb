<%= render 'layouts/kings_auth_layout' do %>
    <%#= render 'shared/form_title', locals: {title: 'Sign In Kings Yangon User'} %>
    <h4 class="custom-font fs-3 fs-md-4 ps-3 fw-semibold text-white">Login</h4>
    <div class="mt-4">
        <%= form_with url: sign_in_kings_yangon_users_path, method: :post do |form| %>
        <!-- Email input -->
        <div data-mdb-input-init class="position-relative mb-4">
          <%= form.text_field :school_id, required: true, class: 'w-100 login-input ' %>
          <i class="bi bi-person position-absolute top-50 end-0 translate-middle-y px-4 text-white"></i>
        </div>

          <!-- Submit button -->
            <div class="actions my-4">
              <%= form.submit "Sign In", class: 'w-100 welcome-sign-in-btn bg-pink-opacity custom-font border-0 ' %>
            </div>

      <% end %>
    </div>
        <%= link_to  root_path, class:'ms-3' do %>
            <i class="bi bi-arrow-left text-white me-2"></i>   
            <span class="text-white">Back</span>
          <% end %>
<% end %>